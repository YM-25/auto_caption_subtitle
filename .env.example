# AutoCaption Pro — optional environment variables
# Copy to .env and set as needed (app does not require .env to run).

# Flask secret key (recommended in production so sessions persist across restarts)
# FLASK_SECRET_KEY=your-secret-key-here

# Whisper model: tiny | base | small | medium | large | large-v2 | large-v3
# Smaller = faster, less accurate; larger = slower, more accurate.
# WHISPER_MODEL=base

# Delete uploaded video and extracted audio after successful processing (saves disk).
# Set to 1 to enable.
# CLEANUP_AFTER_PROCESS=0

# Flask debug mode (0 or 1)
# FLASK_DEBUG=0

# Server port (default 5000)
# PORT=5000
